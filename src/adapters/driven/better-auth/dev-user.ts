// Local-dev-only account, seeded by `npm run db:seed` and used by the
// dev-only one-click login/sign-up buttons (see LoginForm/SignupForm). Not a
// secret — never rendered or seeded in a production build.
export const DEV_USER_EMAIL = "dev@example.com";
export const DEV_USER_PASSWORD = "devpassword123";
export const DEV_USER_NAME = "Dev User";
